#[cfg(test)]
mod tests {

    #[test]
    fn test_tensor_creation() {
        assert_eq!(2, 2);
    }

    #[test]
    fn test_tensor_reshape() {
        assert_eq!(2, 2);
        // Add more assertions to check data correctness
    }

    #[test]
    fn test_tensor_addition() {
        assert_eq!(3, 3);
    }

    // ... other test cases for subtraction, multiplication, division, etc.
}
